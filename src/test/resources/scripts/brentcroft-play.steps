
# create a new browser
Browsers.newBrowser('xyz');

# assign a lambda
xyz.greeting = ( message ) -> c:println( message );

# test the lambda
xyz.greeting( 'Hello world' );

# put another lambda as a runnable
xyz.browser.afters.put(
    '#@*&&^%£$%',
    () -> xyz.greeting( 'good bye cruel world' )
);

# put another lambda as a runnable
xyz.browser.afterAlls.put(
    '*&^%*&^%*^',
    () -> xyz.greeting( 'good bye uncaring universe' )
);

'ok'
